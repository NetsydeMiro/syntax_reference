# standard
if condition
  "this"
else
  "that"

# else if
if condition
  "this"
else if another_condition
  "this2"
else
  "that"

# one-liner
if condition then "this" else "that" 

# return value
return_val = if condition then "this" else "that" 

# unless 
return_val = unless condition then "that" else "this" 

# statement modifiers
return_val = "this" if condition
