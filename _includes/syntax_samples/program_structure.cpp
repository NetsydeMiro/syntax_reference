//  programs require a main function as an entry point
int main(){
  char* entry_point = "required";
}

// command line parameter parsing
int main(int argc, char* argv[]){
  char* program_name, arg1, arg2;

  // argc holds number of arguments
  // argv[0] is always the command executed
  program_name, = argv[0]; arg1 = argv[1]; arg2 = argv[2];
}
