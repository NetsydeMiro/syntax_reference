# scripts require no particular entry point... 
entry_point = 'not required'

# command line parameter parsing
arg1 = ARGV[0]; arg2 = ARGV[1]

# checking to see if this script is the primary entry point
if __FILE__ == $0
  # this only executes if this script is the primary entry point
end
